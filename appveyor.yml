image: Visual Studio 2017

build_script:
  - ps: .\windows\build.ps1

deploy_script:
  - ps: docker login --username "$Env:DOCKER_USERNAME" --password "$Env:DOCKER_PASSWORD"
  - ps: docker push $Env:DOCKER_TAG

clone_depth: 1
test: off

